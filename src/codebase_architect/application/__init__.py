"""Application layer: use cases, orchestration and registries.

Depends on ``domain`` (and ``shared``) only. Wires ports to adapters at the
edges; contains no business rules of its own beyond orchestration.
"""
