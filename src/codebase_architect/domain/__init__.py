"""Domain layer: entities, value objects, domain services and ports.

This package MUST NOT import from any outer layer (application, infrastructure,
api, cli, workers, agents, plugins). It depends only on ``shared`` and the
standard library. Enforced by import-linter.
"""
